{
  "name": "@condrix/client-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Condrix mobile client — React Native app for monitoring, chat, and quick actions",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .expo *.tsbuildinfo"
  },
  "dependencies": {
    "@condrix/protocol": "*",
    "@condrix/client-shared": "*",
    "react": "^18.3.0",
    "react-native": "^0.77.0",
    "expo": "^52.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0"
  }
}
